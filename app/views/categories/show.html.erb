<h1 class="text-center m-4"><%=@category.name%></h1>
    <%if (logged_in && current_user.admin?)%>
        <div class="text-center m-4">
        <%= link_to 'Edit', edit_category_path(@category),class: "btn btn-outline-primary " %> 
         <%= link_to "Delete", category_path(@category), class: "btn btn-outline-danger", method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete  account?" } %>
        </div>
        <%end%>
<h3 class="text-center m-4">Articles</h3>
<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
</div>
<%= render 'articles/article'%>
<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
</div>
